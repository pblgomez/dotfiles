alias v=$EDITOR
alias vi=$EDITOR
alias vim=$EDITOR

# alias vifm="$TERMINAL -e vifm"

# GIT
alias ga='git add .'
alias gc='git commit'
alias gitu='git add . && git commit && git push'
alias gp='git push'
alias gsb='git status -sb'
alias gss='git status -s'
alias gst='git status'

alias pacman='sudo pacman --color auto'
alias update='yay -Syu --noconfirm && sudo snap refresh && sudo flatpak upgrade'

alias cp='cp -i'

alias free='free -m'                                            # Show sizes in MB

alias l=ls
alias l.='ls -A | egrep '\''^\.'\'
alias la='ls -A'
alias ll='ls -la'
alias ls='ls --color=auto'
alias lsa='ls -lah'

alias df='df -hT --exclude=tmpfs --exclude=squashfs --exclude=devtmpfs'

alias mirror='sudo reflector -f 30 -l 30 --number 10 --verbose --save /etc/pacman.d/mirrorlist'
alias mirrora='sudo reflector --latest 50 --number 20 --sort age --save /etc/pacman.d/mirrorlist'
alias mirrord='sudo reflector --latest 50 --number 20 --sort delay --save /etc/pacman.d/mirrorlist'
alias mirrors='sudo reflector --latest 50 --number 20 --sort score --save /etc/pacman.d/mirrorlist'

# A suffix alias allows you to open the file you type in the command line
alias -s {yml,yaml}=$EDITOR

# A global alias enables you to create an alias that is expanded anywhere in the command line, not just at the beginning.
alias -g G='| grep -i'

alias k="kubectl"
alias tf='if ! hash terraform; then terraform-snap.terraform; fi'
